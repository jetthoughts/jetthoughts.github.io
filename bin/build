#!/usr/bin/env sh

set -Eeuo pipefail

if [ -n "${REBUILD:-}" ]; then
  bin/dc build
fi

bin/docked hugo --minify --environment production --forceSyncStatic --cleanDestinationDir --logLevel warn "$@"
