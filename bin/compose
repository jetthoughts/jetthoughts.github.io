#!/usr/bin/env bash

set -eo pipefail

if [[ "$(uname -m)" == arm* && "$(uname)" == Darwin ]]; then
  export DOCKER_DEFAULT_PLATFORM=linux/arm64/v8
fi

docker compose -f .dev/compose.yml -p "${DOCKED_PROJECT_NAME:-showcase}" "$@"
