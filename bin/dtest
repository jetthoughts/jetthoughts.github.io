#!/usr/bin/env bash

set -euo pipefail

rm -rf public-test || true

echo "Building Hugo site for tests (public-test)..."
hugo --noBuildLock --environment production --destination public-test --gc --logLevel warn --baseURL="http://localhost:1313"

# Now run the tests in Docker
echo "Running tests..."
bin/docked t "$@"
