#!/usr/bin/env sh

set -euo pipefail

rm -rf public-dtest || true

echo "Building Hugo site for tests (public-dtest)..."
hugo --noBuildLock --environment production --destination public-dtest --gc --logLevel warn

# Now run the tests in Docker
echo "Running tests..."
bin/docked t "$@"
