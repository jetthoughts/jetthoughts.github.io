#!/usr/bin/env bash

set -euo pipefail

#rm -rf _dest/public-dtest || true

echo "Building Hugo site for tests (public-dtest)..."
#hugo --noBuildLock --environment production --destination _dest/public-dtest --gc --logLevel warn --baseURL="http://localhost:1314"

# Now run the tests in Docker
echo "Running tests..."
bin/docked t "$@"
