#!/usr/bin/env bash

set -euo pipefail

echo "Building Hugo site for tests (public-dtest)..."
hugo --noBuildLock --environment test --gc --cleanDestinationDir --destination _dest/public-dtest --logLevel warn --baseURL="http://localhost:1314"

# Now run the tests in Docker
echo "Running tests..."
bin/docked t "$@"
