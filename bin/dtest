#!/usr/bin/env bash

set -euo pipefail

rm -rf public-dtest || true

echo "Building Hugo site for tests (public-dtest)..."
#hugo --noBuildLock --environment production --destination public-dtest --gc --logLevel warn --baseURL="http://localhost:1313"

# Now run the tests in Docker
echo "Running tests..."
bin/docked t "$@"
