#!/usr/bin/env ruby

require_relative "../lib/sync/sync.rb"

app = App.new(
  args: ARGV,
  logger: Logger.new(STDOUT, level: :debug),
  http_client: DevToClient.new,
  working_dir: "content/blog"
)

Sync.perform(app: app)
