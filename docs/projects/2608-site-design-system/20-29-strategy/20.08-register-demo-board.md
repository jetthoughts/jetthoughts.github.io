# 20.08 — Register lightning-demo board (2026-08-21)

**Decision owner: Paul.** This board carries the pilots, the panel's votes, and
the fable recommendation. Nothing here is decided until Paul says so.

## The three pilots — one anatomy, three skins

Identical copy on all three, verified at DOM level (553 text tokens, one md5;
identical tag and class sequences). The only variable is the register.

| | Live (noindexed staging on production) | Local |
|---|---|---|
| A — Rescue Room (warm humanist) | https://jetthoughts.com/next/pilots/rescue-room/fractional-cto/ | `http://localhost:<port>/next/pilots/rescue-room/fractional-cto/` |
| B — Artifact Editorial (serif editorial) | https://jetthoughts.com/next/pilots/editorial/fractional-cto/ | …`/next/pilots/editorial/fractional-cto/` |
| C — Engineered Precision (ruled technical) | https://jetthoughts.com/next/pilots/precision/fractional-cto/ | …`/next/pilots/precision/fractional-cto/` |

Legacy comparison: https://jetthoughts.com/services/fractional-cto/

## Panel vote (4 personas, sonnet; distinct lenses; forced self-objection)

| Voter | 1st | 2nd | Core argument |
|---|---|---|---|
| Sam — burned founder (trust in 5s) | **B** | C | "reads like a document I can trust, not a pitch I'm being sold" |
| CFO — buyer (price justification) | **C** | B | "the itemized ledger that keeps this firm from ever surprise-billing me" |
| Mobile UX (390px rhythm) | **A** | C | threaded timeline, one-line nav, CTA color constancy start-to-finish |
| Brand strategist (differentiation + scale) | **B** | C | B *performs* "we show our work"; A is the template silhouette to escape |

First choices **B 2 · C 1 · A 1**. C sits in every voter's top two (the safe
choice); **A took zero trust/brand/buyer votes** — warm-rounded consistently
read as template. Strongest self-objections, recorded: B "editorial polish is
what a firm hides behind when substance is missing" (Sam) and "could read as a
media property, not engineers" (Brand); C "am I paying for software or a
person's judgment?" (CFO); A "softer/consumer-app, less rigorous" (Mobile).

## Artifact-level head-to-head (Paul's review request, 2026-08-21 evening)

Paul asked for a review+vote between the two Claude Design artifact sets:
the **Artifact Editorial set** (`8eb98dd6`: homepage + service page — the only
entry proven as a SYSTEM across two pages) vs **Rescue Room** (`3dc819cf`:
service page only). Restricting each recorded ballot to those two:
**Editorial 3 — Rescue Room 1** (Sam, CFO, Brand → Editorial; Mobile → Rescue
Room), plus the fable vote → **Editorial 4–1**. Rescue Room's durable
contributions are its ANATOMY (the shared skeleton) and its mobile rhythm
patterns — both survive regardless via the graft package.

## Fable recommendation: **B — Artifact Editorial, with a graft package**

The tie-breaking observation: the mobile voter's pro-A arguments are **anatomy
patterns, not register properties** — a threaded timeline, one-line nav, and
CTA color constancy graft into any skin. A's weakness (template silhouette) is
intrinsic; B's weaknesses are all fixable inside the register. And B is the
only register that structurally matches the content strategy Paul's own Claude
Design artifacts converged on — selling with documents (redacted audit
exhibits, Friday-report cards) demands an editorial system.

Graft package (applies to B if chosen):
1. **C's ruled-ledger table treatment** for data surfaces (the CFO vote, kept).
2. **A's mobile rhythm patterns**: threaded timeline, one-line nav at 390,
   CTA stays accent-filled through the footer (B currently breaks this).
3. B mobile fixes: reduce all-caps density at small sizes; thread the step
   boxes.

## Defects on the board (status)

- **A+B muted-column**: FIXED — #564 merged (`0c23219e8`); both pilots'
  comparison columns now compute their muted tokens (A 5.95:1, B 5.74:1).
- **Shared mobile table clip** (all three clip column 2 off-edge, no scroll
  affordance): affects all three equally → does not distort the comparison;
  fixed in the winner's graft round, not three times.
- First-fold-only screenshot coverage (below-fold unverified by the suite;
  browser-verified instead) — tracked follow-up.

## Decision protocol

Paul reviews the live links (phone + desktop), reads the votes, then picks:
**A / B / C / none (rerun with new directions)**. The two losing pilots'
stubs+CSS are deleted; the winner's register gets the graft round, then the
service-page flip (`type: next` on the real page) per ADR-0006 §5, then the
homepage build from Paul's Claude Design artifact (claims question pending:
the audit exhibit needs a real source or an honest SAMPLE label).
