# 20.06 — Clean-slate v2 rail: dual-run cutover plan (APPROVED)

**Approved:** Paul, 2026-08-21 (plan-mode approval, this session)
**Supersedes in part:** ADR-0003's propagate-into-legacy *mechanism* for
un-shipped pages. Brand decisions stay in force on the live site until the
competition winner is picked (exploration is fully open — see Track A).
**Reserved sibling:** `20.05` stays reserved for the Phase 2.5 service-page
spec.

---

## Why (one paragraph)

Paul's direction: the site must read as a **top-tier service with a top-tier
UI**, and the current CSS — recovered from an obfuscated export — is not the
foundation to build that on. The session evidence agrees: every born-clean page
shipped without reverts (blog rebuild 0 fl-lines, blog-single 2, course 11,
vibe-code-rescue, friday-report), while every legacy-propagation attempt failed
(1a.4 footer reverted; 41-rule eyebrow sweep reverted on AA; #540's first
attempt created the seam it removed; buttons = 780 sites, never started). So:
**stop patching legacy; build v2 clean; run both; cut over page by page**, with
a non-indexed `/next/` path on production for live testing.

## Decisions of record (Paul, 2026-08-21)

| Decision | Choice |
|---|---|
| Design competition size | **2 directions** (third only from rejection feedback) |
| Exploration constraints | **FULLY OPEN — rebrand allowed.** Only claims-canon, company name, no-stock, anti-goals constrain. Ruby/LIGHT/logo may be reproposed. |
| Tools | **Claude Design and stitch COLLABORATE** — stitch generates here; the same brief runs in a NEW Claude Design project with Paul; outputs cross-critique. NOT the old "Rescue Room" project (it renders extraction — that's why previews looked like course reuse). |
| Staging | `/next/` non-indexed path on production; sunset after two clean flips |
| Legacy | Frozen (defect fixes only). 780 button sites + 39 blacks DROPPED as work-done-twice. |
| Sub-agent models | sonnet/haiku; Fable reserved for main loop + critical judgment |

## The two tracks

### Track A — design collaboration (no repo code; parallel with any PR)

1. **Brief** (unanchored, unconstrained): "top-service impression / TOP UI",
   ICP 90.10 (burned non-technical founder), page set = homepage +
   single-service + contact + about, anti-goals = no logo walls, no carousels,
   no extraction from the current site, no stock photography. **Zero
   screenshots of the current site. No palette/typography locks.**
2. **Direction 1 exists**: "Artifact Editorial" — Stitch project
   `4487418121858436329` (warm paper `#fbf9f8`, Source Serif 4 + Hanken
   Grotesk, sharp 0px corners, evidence-over-assertion; rejected logo
   walls/carousels unprompted). *Disclosure: generated under a ruby-locked
   brief before the fully-open decision.*
3. **Direction 2**: stitch-design run, opposed bias — premium/enterprise-trust,
   palette/identity free. Same brief handed to Paul for a NEW Claude Design
   project; outputs cross-pollinate.
4. **Judging**: `/impeccable` critique + ICP cold-eyes panel (lenses:
   first-5-seconds trust, purchase-intent CTA clarity, mobile). Paul sees one
   side-by-side board per direction; **Paul picks**.
5. **Winner** → stitch-loop iterates the 4-page set → `docs/design/DESIGN-v2.md`
   + tokens + component inventory. If the winner deviates from ruby/LIGHT,
   ADR-0006 supersedes ADR-0003's brand decisions too and shipped blog/course
   surfaces join the re-skin queue (accepted cost).

### Track B — the repo rail (WIP=1, one PR at a time)

**PR-0 (this sprint): 2.3 course eyebrow** — canonical eyebrow above the course
H1 reading the `module-N` tag. Decided by three-lens panel before the pivot;
unaffected by it. NOT in scope: rewriting lesson titles.

**PR-1: v2 rail + qtest fix** (~150–250 code lines):
- `bin/qtest` `pages_for`: add `^layouts/next/` → `next-pilot`, terminal
  `^layouts/` → `:all` (closes a TODAY false-green: root-layouts-only diffs
  currently exit 0 having tested nothing — `bin/qtest:69-78`), + `PAGE_TESTS`
  key.
- `content/next/_index.md` — `cascade: {noindex: true, private: true}`.
  Plumbing exists: `enhanced-meta-tags.html:71-73` emits noindex from
  frontmatter; `layouts/sitemap.xml:10` excludes `private: true`. **No
  robots.txt disallow** (it would hide the noindex meta from crawlers).
- `layouts/next/baseof.html` (~80 lines): functional partials verbatim
  (enhanced-meta-tags, 5 schema partials, `page/analytics.html` — every
  conversion event is attribute-driven, zero fl-* deps — favicons,
  service-worker); DROP the 7 fl-* body classes + `.fl-page` wrapper; REUSE
  clean chrome verbatim (`header-content.html`, `footer-content.html`,
  `navigation.js` — all 0 fl-*, class names already purge-shielded).
  Canonical → source page when `source_page` set.
- `layouts/next/single.html`: `site.GetPage .Params.source_page` — **stub owns
  only the URL; copy has one source of truth.**
- `themes/beaver/assets/css/pages/next-pilot.css`: tokens + reset only —
  deliberately no `style.css`/`586.css`/`base-4.min.css`. Unique `bundleName`
  (Concat races on collisions). No new purge shield needed (`hugo_stats.json`
  includes /next/ pages).
- Stub `content/next/services/fractional-cto.md` (pilot = single-service:
  highest purchase intent, ungated, worst ICP betrayal per panel).
- Test `test_next_pilot`: desktop+mobile screenshots + noindex-meta assertion +
  sitemap-absence assertion.
- `docs/adr/0006-*.md` (dual-run clean-slate: supersession scope, freeze,
  homepage-gate carve-out, /next/ sunset) + ownership-map row +
  `docs/workflows/new-page.md` v2 fork (its step 1 currently declares the FL
  shell "load-bearing").

**PR-2: pilot styled per winning direction.** Only `layouts/next/single.html`,
`next-pilot.css`, baselines. Verification = `/impeccable` pass + Paul on
`/next/services/fractional-cto/` on real devices + browser-measured AA/seam
(#540 protocol) + canon/voice on surfaced copy.

**PR-3: flip** (tiny, reversible). Real page gains `type: next`; permalink
unchanged → no redirects; stub deleted; page-key baselines re-recorded via CI
dispatch. Revert = two frontmatter lines.

**Then iterate**: remaining service pages → contact/about. Homepage v2 is
buildable at `/next/` any time; the PUBLIC flip honors the ≈2026-09-17
engagement-read gate (first action there: confirm the `e1fa5409d` deploy date —
still unknown). After two clean flips, drop `/next/` staging.

## Gates (every PR)

Feature branch · rebase-not-merge · author≠verifier per stage (verifiers on
sonnet) · internal reviewers per stage, `/codex:review` once pre-merge for
user-facing changes; docs-only ships on internal review + CI · OKF sync in the
same commit · browser-measured AA + seams on anything touching a rendered page.

---

## COLD-SESSION STATE LEDGER — update this section every session

> A separate session finishing this work starts HERE. Update statuses in the
> same commit as the work they describe. Status comes from git for code; this
> table is the pointer, not the proof.

| Item | Status 2026-08-21 | Next action |
|---|---|---|
| Strategy approved + persisted | DONE (this doc) | — |
| PR-0: 2.3 course eyebrow | BUILT + browser-verified (25 lessons render, 5.13:1 AA); baselines see PR handoff | opus diff review -> push -> CI Linux baseline dispatch (screen for course keys only) -> codex -> merge |
| Track A: direction 2 (stitch) | NOT STARTED | run stitch-design with the §Track A brief, project `4487418121858436329` or new |
| Track A: Claude Design handoff | NOT STARTED | write brief doc for Paul; he runs it in a NEW Claude Design project |
| Track A: judging board | BLOCKED on direction 2 | /impeccable + ICP panel (sonnet), board for Paul |
| PR-1: v2 rail | BLOCKED on PR-0 merge (WIP=1) | execute §PR-1 list verbatim |
| PR-2: styled pilot | BLOCKED on PR-1 + Track A winner | — |
| PR-3: flip | BLOCKED on PR-2 + Paul live review | — |
| 20.05 service-page spec | NOT STARTED (hive specialists idle) | content model (month-one card, decision table, timeline) survives any design winner |
| Homepage read gate | WAITING ≈2026-09-17 | first: confirm `e1fa5409d` deploy date |

**Standing rules for whoever picks this up:** WIP=1 on repo PRs; sub-agents on
sonnet/haiku (Fable = critical only); background sub-agents go idle silently —
run gating reviewers foreground with "YOUR FINAL MESSAGE IS THE REPORT";
measure on the live render, never source; run the suite at the MERGE BASE
before trusting green.
