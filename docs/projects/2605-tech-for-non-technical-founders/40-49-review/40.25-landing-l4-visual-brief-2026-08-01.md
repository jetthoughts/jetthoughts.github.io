# 40.25 — Landing L4-visual brief (shuffle-demo voter synthesis, 2026-08-01)

Input: Paul's 3 shuffle.dev demos (preference order 1>2>3; full-page captures
in tmp/shuffle-{1,2,3}-full.jpeg), voted by 3 independent fable lenses
(taste / conversion / feasibility). Nav+footers excluded per Paul. This brief
executes ON TOP of the L3 restructure (40.24), same branch
`course-landing-l3`, AFTER the l3-reviewer verdict. Paul has NOT yet reviewed
the L3 version — everything here is a pattern overlay he can trim at PR.

Scope guard (campaign window): `themes/beaver/assets/css/pages/course-list.css`
+ root `layouts/course/list.html` + `_index.md` markup ONLY (bundle isolation
verified: course-list bundle is exclusive to the landing). No shared CSS, no
new JS. All copy is contracted by 40.24 — patterns restyle existing content.

## Adopt list (vote-ranked, commit order per feasibility)

**Commit V1 — course-list.css only (~150 lines, cheapest 4 patterns):**
1. Numbered mistake-card grid (3/3 voters; demos 3+1): CSS counters 01-05 in
   ruby on `ul.mistake-list`, card grid. Fixes the five-mistakes F-pattern.
2. Deliverable-first module cards (3/3): elevation on `.module-card`,
   right-aligned "Output:" badge (`__output`), highlighted "You leave with ✓"
   panel (`__leave`). PROTECTED copy, zero text changes.
3. Dark closing endcap band (3/3): `.course-endcap` → obsidian breakout band,
   ruby "Start Lesson 1.1 →", subtle ruby radial glow (JetVelocity token).
   Existing endcap content unchanged (its copy-link button shipped with C1
   analytics — NOT the P4 share line; keep).
4. Eyebrow + headline scaffold (2/3): ruby uppercase tracked eyebrow above
   each section H2 via CSS (`::before` from a data-attr or per-section
   class), Space Grotesk headline rhythm. Hero chips+CTA get the same polish
   pass (conversion's #1 — unit already exists post-L3).

**Commit V2 — _index.md markup (landing-owned) + CSS:**
5. Module-map timeline rail (demo 1): wrapper div + `::before` rail; skip
   the node dots if mobile alignment fights back.
6. Mid-page inline CTA card as the 6th cell of the mistakes grid (demo 3):
   dark inverted card "jump to the module map" → existing `#module-map`
   anchor. New markup allowed (it's a door, not new claims — copy ≤15 words,
   voice-checked).
7. Module-card two-col: chapters left, leave-with panel right
   (grid-template-areas; single column <768px).

**Commit V3 — the ONE bold move, separate + revertable (demos 1+3, taste #1):**
8. Dark hero course-artifact card (obsidian island: ruby "FREE COURSE"
   eyebrow, title, stat chips from course-stat, "you leave with" line) as a
   hero aside in list.html. DECISION BAKED IN: the card REPLACES the cover
   figure in the hero (never both). Hero container widens beyond 720px.
   Flagged at PR as the drop-if-Paul-dislikes commit.

## Killed by feasibility (2-vs-1 resolved: constraints beat taste)

- Modules 3-5 collapse ("Expand ↓") — needs JS/details restructure of
  contracted markup + anchor risk. Revisit post-campaign with scroll data.
- Two-tone gradient H1 keyword — `.Title` is one frontmatter string reused
  by SEO/meta; forking title rendering is fragile. Defer to V3-B/W4 where
  the template is open anyway.
- Dark NOT-cover section — max-dark-footprint concern + 7-identical-cards
  F-pattern warning. Defer.
- Search hub, featured grid, trigger tables, expiry-flavored badges, demo
  copy language — permanent kills (identity/contract/dark-pattern).

## Still Paul's call (2nd independent signal arrived)
- "First verdict in 2-3 weeks" hero chip — demo 3 carries the same chip
  natively ("Validation Pace 2-3 weeks"); two panels have now flagged it.

## Gates per commit
qtest --changed green + landing baseline pair (macos) updated in-commit;
4-criteria visual score written in the commit message; scroll gate both
viewports after V3; impeccable re-critique (cold, not self-scored) after all
commits — beat 25/36; test+dtest pair at PR prep; linux baselines via CI.
