# Course sprints C0-C3 - groomed queue (2026-08-20)

**Status: GROOMED, ready for cold-session execution.** Scheduled by the manager
session of 2026-08-20 (analytics read + fix sweep, PR #474). Sequencing honors
the 2026-08-13 panel ruling: **arrival first, polish opportunistic** - C1 runs
before C2/C3; C0 is parallel-safe engineering.

**How to execute (any agent, no coordinator context needed):**

1. WIP=1 - one task at a time, finish + gates green before the next.
2. Feature branch off master + ONE PR per sprint (never direct-to-master).
   Committing agents work in worktree isolation ([[feedback-workflow-writers-need-worktrees]]).
3. Read before touching course content: `60-69-policies/60.01-course-editing-policies.md`
   (BLOCKING), `.okf/content/claims-canon.md`, `.okf/content/voice-rules.md`,
   `docs/90-99-content-strategy/90.20-brick-and-claims-audit-2026-08-17-reference.md`
   (deliberate exceptions - do NOT "fix" them).
4. **C6 no-touch list** (40.23): Mia M1 walkthrough, 2.5 Dana transcript, the 3
   decision-fork *bodies* incl. 4.1 mermaid, 1.4 as v2 exemplar, canon numbers,
   90/10 footer.
5. Gates by change class: content-only = `bin/hugo-build` + rendered scroll gate
   on edited pages; templates/CSS = + `bin/qtest --changed` per commit and the
   FULL pair (`bin/test` + `bin/dtest`, CI-recorded Linux baselines - NEVER
   local dtest on ARM, see `.okf/build/test-gates.md`) at PR prep.
6. New visuals: verify at 1280x800 AND 390x844, `img.complete` before judging,
   score look/readable/earns-scroll/helpful - any NO on the last two = rollback.
   Hand SVGs: fit text for the cursive FALLBACK (~0.55em/char), not Caveat -
   `.okf/design/house-visual-spec.md`. Mermaid: `bin/render-mermaid <page>`;
   LR layouts fail at 390px - prefer TD or card rows.
7. Every task ends with: tracker checkbox here, dated `.okf/log.md` entry if a
   durable learning landed, handback message with `http://localhost:<port>/<path>/`
   links for every edited page.

---

## Sprint C1 - "Arrival wave" (run first)

Constraint of record: arrival, not content (Aug-14 metrics read +
Aug-20 GA/GSC/Clarity read, both in `TASK-TRACKER.md`). Course content is
frozen except defect fixes.

### C1.1 Revise 3 LinkedIn lane drafts to the idea-first doctrine

- **Why:** queue item 0's named next wave. 14 drafts sit `approved` under the
  OLD story-first doctrine; the pipeline flipped to idea-first on 2026-08-13.
- **Files:** pick 3 by scheduled order from `linkedin-posts/course-promo/`
  (start: `week1-wed-first-move-poll.md`, `week1-fri-why-i-wrote-it.md`,
  `week2-mon-friends-politely-lying.md`).
- **Doctrine:** `docs/workflows/linkedin-post-pipeline.md` (idea-first, deliver
  the point; AI-score rubric) + `linkedin-posts/README.md` +
  `linkedin-posts/reference-examples.md`. Voice: Register B committed hammering,
  no beat-marking ([[feedback-register-b-fried-style]]).
- **Steps:** revise body only (frontmatter v2 stays), run `reflexion-reflect`
  self-critique, then a 2-critic cold-eyes pass (AI-feel + voice-enforcer),
  fix convergent findings, set `status: approved` + note `revised: idea-first
  2026-MM-DD` in frontmatter.
- **Done:** 3 drafts revised, critic findings recorded in the PR description,
  handback ends with the 3 preview links
  `http://localhost:<port>/linkedin/course-promo/<slug>/`. Posting stays
  Paul-gated - do NOT schedule.
- **Trap:** UTM links already correct - do not touch them. One anecdote/number
  may appear in two posts only if the STORY differs (see .okf/log 2026-08-19
  learning #7).

### C1.2 Fabricated-fact fixes - prepare decisions, apply Paul's pick

- **Why:** 3 findings held for owner judgment in the Aug-20 sweep
  (`TASK-TRACKER.md` Aug-20 note).
- **The three:**
  1. `content/course/.../five-tech-words-stop-nodding-at/index.md` ~L94 -
     "A B2B HealthTech founder we shipped for in Q2 2025" + $14K/$58K numbers.
     Options: (a) cite the real engagement if one exists (ask Paul), (b) drop
     "we shipped for" + append the standard composite disclaimer (already used
     at L53 of the same page), (c) cut the anecdote.
  2. `content/course/.../sow-reading-guide/index.md` L138 + L176 - "$78K for
     $400" presented as a real founder's fact while the L37 opener is a
     second-person hypothetical. Options: (a) de-story both refs ("would have
     saved five figures for $400"), (b) restore a disclaimed composite opener.
  3. `content/course/.../paid-pilot-charge-before-ship/index.md` ~L55 - the
     12%-vs-65% conversion bar chart SVG (stat lives inside artwork; text
     ratchets can't see it). Options: (a) source it (none known in-repo),
     (b) re-render the exhibit without the invented percentages (qualitative
     "free pilots ghost / paid pilots convert"), (c) drop the exhibit.
- **Steps:** prepare the concrete diff for the RECOMMENDED option per item
  (b / a / b respectively), post the three diffs to Paul in one message, apply
  what he picks. If no reply in the session, leave the prepared diffs in the
  PR description - do NOT apply unasked (claims are owner-gated).
- **Gates:** content-only class; scroll-gate the 3 pages; if the SVG is
  re-rendered, full new-media visual gate.

### C1.3 Five-day campaign read scaffold

- **Why:** item 0's kill-criteria are untestable while
  `linkedin-posts/metrics-ledger.md` has an empty row (7+ days overdue).
- **Steps:** (1) extend the ledger with empty rows for every `scheduled`/
  `posted` draft, one row per post, correct slugs/lanes; (2) write
  `linkedin-posts/campaign-read-2026-08.md`: the 5-day read template - per-post
  impressions/reactions/comments/icp_replies columns, the lane kill-criteria
  quoted verbatim from `docs/workflows/linkedin-icp-validation-plan.md`, and a
  "verdict" section left blank; (3) list the exact numbers Paul must paste
  (LinkedIn analytics per post) at the top.
- **Done:** ledger rows exist, read doc exists, handback tells Paul exactly
  which numbers to paste where (that part is his - agents have no LI access).

### C1.4 Needs-Paul checklist (no agent work - keep visible)

- GA4 admin: mark `contact_cta_click` a key event (1 click).
- Fill the C1.3 ledger numbers from LinkedIn analytics.
- Pick options for the three C1.2 items if not picked in-session.

---

## Sprint C2 - "Reader-experience visual wave"

Paul (2026-08-20): text-only posts risk losing the reader. Pattern proven on
`switch-dev-shops` (mermaid timeline + decision cards) and `retros` (hand-drawn
exhibits) in PR #474 - replicate, don't reinvent.

### C2.1 Visual pass on the remaining text-only Wave G posts - ✅ DONE 2026-08-20 (PR #477)

All four posts shipped one hero exhibit each, 4/4 on the visual gate at both
viewports. Mid-body second exhibits deliberately declined on all four: the
remaining sections are one idea in two paragraphs, so a second visual would
restate the H2. An independent critic caught a **legally wrong** exhibit
(stated flatly that whoever typed the code keeps copyright, dropping the
employee exception the post's own first line carries) - fixed before merge.


- **Scope (4 posts, 1-2 exhibits each, in this order):**
  `content/blog/dev-shop-sla-requirements-checklist/` (severity-response table
  or SLA-clause card), `content/blog/dev-shop-contract-code-ownership/`
  (4-clause map or work-for-hire decision aid),
  `content/blog/cheap-developers-expensive-without-cto-review/` (the
  pay-twice cost flow), `content/blog/asked-simple-admin-panel-built-spaceship/`
  (spec-with-blanks vs spaceship contrast card).
- **Per post:** ONE hero-capable exhibit in the first fold + optionally one
  mid-body break. Mermaid via `bin/render-mermaid` (TD or LR card-rows only)
  OR hand SVG per house spec - match what the content wants, not habit.
- **Gates:** full new-media visual gate per exhibit (both viewports, 4 scores
  in the commit message); content-only class otherwise. Alt text carries the
  full reading of the exhibit.
- **Trap:** exhibits are subject to the banned-strings ratchet too - no
  invented stats inside artwork; dashes are "-" in SVG text.

### C2.2 artifact-trail quick win (5 SVGs, one pattern)

- **Why:** tracker's parked section names it the quick win: the 5
  `module-{1-5}-walkthrough-mia/artifact-trail.svg` share one layout; one
  redraw pattern clears all 5.
- **Policy (BLOCKING):** render-gate per image, NOT metric-gate
  ([[feedback-legibility-fix-not-redesign]]) - redraw to the refactoring.fm
  2-row-grid style of `friday-demo-template/friday-demo-timeline.svg`
  (commit 7e87b1a7 is the exemplar), then eyeball each at 390px; if a redraw
  reads WORSE than its original, keep the original and record why.
- **Gates:** new-media visual gate each; scroll-gate the 5 walkthrough pages.
- **Out of scope:** the other 31 report-only candidates. Do NOT batch-convert
  (that was item 18, ABANDONED - PR #445).
- **DONE 2026-08-20.** All five redrawn (M1 by the coordinator as the pattern,
  M2-M5 by agent), all five clear `bin/check-svg-floor`, all 4/4 on the visual
  gate at a true 390x844 device render. M3/M5 have 4 cards not 5, so they took a
  2x2 rather than 3+2 - following the originals' own wider-card logic.
- **Follow-up candidate, deliberately NOT done here:** the five trails do not
  share colour semantics. M2 puts green on a BUILD verdict and a Money card with
  purple on the prototype, M4 puts purple on Live MVP, only M5 reserves green for
  the payoff. The house spec says green is money/success only. The redraw was
  scoped to layout and type size, and recolouring moves MEANING, so it was
  correctly left alone - but a palette-unification pass across the five is a real
  open item. Render-gate it the same way; do not assume the spec's letter beats a
  working exhibit.

---

## Sprint C3 - "W4 media/template wave" (item 13, TEMPLATE class)

The one remaining wave from plan 20.15. Demoted to opportunistic on
2026-08-13; scheduled here as the LAST sprint so arrival work never queues
behind it. Full archived grooming: `_ARCHIVED_TASK-TRACKER-2026-07.md` @L297.

### C3.1 Responsive-table overflow containers - ✅ DONE 2026-08-20 (PR #481)

Reproduced worse than recorded: a table 507px wide inside a 354px column with
`overflow-x: visible` on the parent - no scroll container at all, so the whole
page dragged sideways. One mobile-scoped media query in `single-post.css`,
which both `single.html` and `course/single.html` already load, covers blog +
course + every sibling reference table. Desktop verified untouched.


- **Why:** W2 scroll gate found 390px overflow on
  `reference/fractional-cto-sow-reference` (4-col table, 25px over) and
  `first-paying-customer-operating-kit` (51px over, pre-existing since W1).
  Fix was frozen "until this wave" - this is the wave.
- **What:** shared course-single CSS rule wrapping course tables in
  `overflow-x:auto` scroll containers (page body must never scroll
  horizontally). Clears both named pages + sibling reference tables at once.
- **Gates:** `bin/qtest --changed` per commit; before/after 390px screenshots
  of both named pages in the PR.

### C3.2 + C3.3 - ❌ CLOSED UNBUILT 2026-08-20 (superseded, not forgotten)

**Original scope.** C3.2: new course-single.css + single.html, walkthrough
visual hooks, TL;DR accent, per wave plan
`20-29-strategy/20.15-course-improvement-wave-plan-2026-08.md` §W4. C3.3:
mid-body visuals for 1.2/1.3/1.5 and the 5.7 mermaid.

**Why closed rather than carried.** These are the last items of a CLOSED wave
plan, and they are polish on a course that has a distribution problem, not a
quality problem. Three independent reads now agree:

- The 2026-08-13 manager panel demoted them to "opportunistic" - the constraint
  is arrival/reach, not polish.
- The Aug-14 metrics read said aim template work at M2-M3, because M4/M5 are
  "pages almost no one reaches".
- The Aug-20 GA/GSC/Clarity pull: ~12 Google organic + ~7 AI-assistant + ~4
  LinkedIn course arrivals in 28 days; course URLs at positions 5-13 with 2
  clicks; funnel events still near-floor (`start_course` 3, `glossary` 1).

Rebuilding the course template for that traffic is work whose payoff is
gated on a number that has not moved. C3.1 was different and WAS built - a
390px overflow is a defect on every visitor, not an improvement for a
hypothetical one.

**A dangling "someday" item is a real cost.** It got re-litigated in three
separate sessions. Closing it with a trigger is cheaper than carrying it.

**Reopen when:** course arrivals from any single channel exceed ~50/28d, OR
the funnel shows sustained M2-M3 read-depth with a drop at the template layer
(not at arrival). At that point re-groom from 20.15 §W4 rather than from this
file - the spec will be stale.

**Do NOT reopen** merely because the items exist. That is the exact reasoning
this entry closes off.

---

## Sprint C0 - "Ratchet hardening" (engineering, parallel-safe, any time)

### C0.1 Blog text ratchet on rendered output

- **Why:** `marketing_copy_test` doesn't glob `content/blog/**` and reads
  source, not rendered output - every late defect of 2026-08-14 was invisible
  to it, and the Aug-20 sweep found live claims defects in blog posts it
  never sees. Queued as `docs/20-29-testing-qa/20.10` §3b P0-4.
- **What:** a second pass in `test/unit/marketing_copy_test.rb` (or sibling)
  reading `_dest/public-dev/**/*.html` for the banned-strings + canon set,
  scoped to blog+course+services HTML. Naive grep over built files is fine
  (ponytail); runtime budget <10s; skip dev.to-imported posts' third-party
  stats (their numbers belong to their authors - allowlist by `source: dev_to`
  slugs or path list).
- **Ratchet discipline:** start with the CURRENT violation count as the
  baseline if nonzero (ratchet, not big-bang) - fail only on regressions.
- **Gates:** `bin/rake test` green; the new test red-green demonstrated in the
  PR (introduce a banned string in a scratch build, show it caught).

---

## Not scheduled (explicitly, so nobody re-litigates)

- **Item 7** Wave G Batch 2 publish - BLOCKED on the blog->course bridge gate.
- **Item 14** reader-mode - Paul-postponed.
- **External validation pilot** - post-course-completion.
- **Visible progress tracker** - PAUL-GATED on the 30-min Clarity check.
- **SEO/title work of any kind** - seo-review-2026-08-13 §6 #5 (falsified).
- **31 remaining SVG floor candidates** - opportunistic-only per policy.
