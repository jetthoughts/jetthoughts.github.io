# 20.15 — Course improvement wave plan (2026-08)

Synthesis of the open 40.xx research threads into 5 sequenced waves — no new
ideation (the corpus already triangulated: 40.19 premium swarm, ADR 30.09
panel, 40.05 ICP review, 40.22 structural audit). One wave per session
(WIP=1), in this order. Canonical board: TASK-TRACKER (items 9-13).

**Ordering constraints:** campaign window Aug 1-14 (B1/B2 + Aug-14 metrics
read) → L3 ships BEFORE 2026-08-14; NO shared template/CSS churn inside the
window (V3-B parked post-Aug-14). Gates per CLAUDE.md: content-only →
hugo-build + scroll gate + cold-eyes personas; template/CSS → qtest per
commit, test+dtest at PR prep. "Pilot" = internal review.

| Wave | Scope | Research | Status |
|---|---|---|---|
| **W1** Landing L2→L3 | Multi-critic section-order punch-list (NO edits), then surgical L3 restructure: "Going further" off acquisition page, promote already-building route, expand authorship footnote, break 646w prose tail. Re-run impeccable critique — beat 25/36 baseline | 40.21 + landing track | **UNBLOCKED** (L1 = PR #416 MERGED); deadline 2026-08-14 |
| **W2** v2 format fix + deep audit | 11 off-scaffold lessons (per-lesson miss-vs-exception call), split hire-track ref (5,558w), then runbook Phases 3-6 + cold-eyes on edited chapters | 40.22 | tracker item 10 |
| **W3** Visual V3-A | Exhibit spec → A/B exemplar → 4 M1 SVG redesigns → eye-test doc → **PAUL'S STYLE CALL in ADR** (blocks wider rollout). Program map v1 alongside. Sprint-Y classify-first audit either way | ADR 30.09 | campaign-safe (content-scoped) |
| **W4** V3-B + media P1 | course-single.css + single.html wiring; walkthrough visual hooks, 1.2/1.3/1.5 mid-body visuals, 5.7 mermaid horizontal, TL;DR accent | ADR 30.09 + media backlog | **post-Aug-14 only** |
| **W5** Completion mechanics + content strategy | Path-finder audit of Next:/forks vs course_sequence.yaml; strengthen the 3 decision forks (2.5 / 4.1 / 5.6) as diagnose→route moments; compress the over-length Going Further set (3,205w / 3,150w / 2,869w / 2,733w / 2,658w) per 30.03 bands; verify every Success check is observable-behavior measurable | 2026-07-30 strategy review ("completion mechanics beat more media") + learn-with-coursera path-finder pattern | progress tracker stays GATED on Paul's 30-min Clarity check |

**Not doing:** fresh persona fan-outs pre-W1/W2 (corpus already has them; L2
is the one new critic pass), Coursera integration (pattern only), template
churn before Aug 14, external pilot recruitment (Paul's desk), progress
tracker before the Clarity gate.

**Cold-session pickup:** open TASK-TRACKER → take the lowest un-done wave
item → its Research column names the artifact with full findings.

---

## W1 + W1.5 retrospective (2026-08-01, Start-Stop-Continue)

Scope: PRs #428 (L2→L5, 15 commits) + #429 (layout convergence, 3 commits).
Outcome: shipped; cold critique 29/36 vs 25/36 baseline; Paul's final
verdict "layout + section background colors good" after a W1.5 correction
round his feedback forced.

### Continue (worked)
- Contract-doc → executor → 4-eyes reviewer → cold critic chain: every
  stage caught real defects the previous one missed.
- Component-spec extraction from the reference's live DOM
  (`docs/design-system/course-landing-components-2026-08.md`) - W1.5
  converged in 3 commits because the target was already written down.
- Honest-dtest-from-main discipline + the vacuous-worktree tell (now in
  `.okf/build/test-gates.md`).
- Merge-gate voting panels: fast (~5 min parallel), auditable, zero
  blockers missed at the code/contract level.

### Stop (failed - the load-bearing lesson)
- **Shipping component-modern without page-gestalt verification.** W1's
  panel unanimously SHIPped a page Paul called "terrible": every voter
  judged sections/components against the spec; nobody judged the FULL-PAGE
  side-by-side against the reference at equal zoom. The known "section-band
  rhythm deferred" gap was self-accepted as polish when it was the dominant
  perceptual factor. Rule change: for any page redesign, full-page
  reference side-by-side is a BLOCKING gate, and an accumulating
  "accepted deviations" list gets re-validated with Paul, not self-accepted.
- **Proxying Paul's taste through panels.** When acceptance = a person's
  taste, show the person one image early (W1.5 did; verdict in minutes).
- **Ops frictions that cost retries:** persistent-cwd git mistakes (3x:
  rebase/commit/detach landed in the wrong checkout - always absolute-path
  the target repo in git commands); piping gate commands through `tail`
  masked an OOM exit 137 (capture PIPESTATUS); background voters' final
  reports can be lost on idle - re-request via SendMessage is the fix.
- **Sequencing:** condensing content (L5) before the layout system existed
  (W1.5) meant density was judged in the wrong frame. Layout system first,
  content density second.

### Action items (carried into W2 grooming)
1. [x] Reference side-by-side = blocking gate → recorded here + wave briefs
   must name it (W3/W4 especially).
2. [ ] W2 grooming re-audits the 11-lesson off-scaffold list against the
   CURRENT tree before dispatch (audit-premise rule).
3. [ ] Expander-density reduction (Paul: "maybe too many expand-to-read-more")
   → top candidate at the next visual touch; groomed into W3 unless W2's
   scope touches the landing anyway.

---

## W2 retrospective (2026-08-01, Start-Stop-Continue)

Scope: PR #431 (19 commits) - v2-format scaffold fixes across 25 lessons +
hire-track split + deep audit + 4-persona review. Merged clean.

### Continue (worked)
- **Grooming-before-dispatch earned its keep**: re-audit found 2 new defects
  and struck 1 stale item vs the day-old 40.22 list. The mandate's retro→groom
  step is not ceremony.
- **Parallel worktree fan-out (T1-T5 on disjoint files) → serialized sweeps
  (T6/T7/T8)**: 5 workers finished fast, zero merge conflicts, and the
  serialized audit-after-merge caught cross-file issues no single fixer saw.
- **Convention broadcast mid-flight**: T2 set the outcome-line placement, one
  SendMessage converged T1+T3 - cheaper than a fix-up pass.
- **The layered review (audit → 4-persona panel → synthesized fix → re-check)
  caught a real HIGH defect** (kit DPA excerpt dropped the refund half) that
  every mechanical gate passed.

### Stop (the load-bearing lessons)
- **Screenshot-guardian agents were unreliable twice**: gatekeeper 1 reported
  a partial walk as complete and asserted 4 "defects"; both walkers flagged
  SVG failures that were HTTP-200 non-defects (they probed a theme icon, not
  the lesson SVG, and read lazy images pre-scroll). Rule: a scroll-gate report
  is not trusted until it carries the EVIDENCE asked for (HTTP status for asset
  claims, a named element + the control-page measurement for overflow claims).
  The manager verifies asset/overflow claims directly (curl, diff-logic) before
  acting - do not block or dismiss on an agent's bare assertion.
- **Relocating prose in a split carries its pre-modern-gate debt**: the new
  fractional-cto-sow-reference concentrated ~half the panel findings because
  moved sections had never passed the current voice/gloss gates. Rule: a page
  SPLIT schedules a voice+gloss pass on the moved content, same wave.
- **Append-only .okf/log.md conflicts on every worktree rebase**: T5's log
  entry vs master's newer entries. Cheap to resolve (keep both, newest-first)
  but predictable - worth a merge=union .gitattributes someday (backlog).

### Action items (into W3 grooming)
1. [ ] Scroll-gate briefs must demand evidence-per-claim (HTTP status, named
   element, control measurement); manager spot-verifies before ship/block.
2. [ ] Any page-split task includes a same-wave voice+gloss pass on moved prose.
3. [ ] W4 owns the responsive-table CSS (tracker item 13) - the mobile overflow
   W2 surfaced is a shared-CSS fix, correctly deferred.

---

## W1.6 retrospective (2026-08-01, brief - Paul-inserted landing wave)

Scope: PR #432 (1 commit, Opus executor). Expanders 7→0, text -21%. Paul: "ship it".

- **Continue**: single-executor landing pass with Paul's taste gate at PR (W1.5 pattern) works well for taste-led visual waves; reference-capture-first + side-by-side gate (W1 retro lesson) held.
- **Stop / learned**: (1) **Fable quota exhausted mid-session** - the first W1.6 executor died on it having done zero work; agent model fell back to Opus. For the rest of THIS session, dispatch agents on Opus/Sonnet, not Fable. (2) **The landing visual-regression test is hero-fold-scoped** - a full below-fold redesign passed the pixel suite untouched; the chrome-devtools scroll gate is what actually covers below-fold. Do not read a green landing pixel test as "the whole landing is verified". (3) removing expanders surfaces all hidden text - "fewer expanders" and "less text" can conflict; the reference's answer is to RELOCATE depth off-page, not just unhide it.
- **Action into W3**: agents on Opus/Sonnet this session; module-map compaction is a logged lever (item 15) if Paul flags length again.

---

## W3 retrospective (2026-08-01, Start-Stop-Continue)

Scope: PR #433 (Visual System V3-A, M1 pilot). Groomer + T1-T5 all on Opus/Sonnet (Fable out). Paul chose O2.

### Continue (worked)
- **Spec-first (T1) before any pixel** gave T2/T5 an objective rubric + a MEASURED floor formula, not vibes - the 5 redesigns came out consistent because the template was written down first.
- **A/B exemplar as the decision artifact**: one exhibit, both styles, same message → Paul decided in one look. Isolating the single variable (style) is why the call was clean.
- **The style-call gate held correctly** - W3 was the first design call so it went to Paul; per his new standing method, future style calls run a panel + competitor grounding and decide autonomously.
- **Sprint-Y found a real defect** the style question would have hidden: 4/5 M1 SVGs failed the mobile floor TODAY. A "make it prettier" wave delivered a legibility fix.

### Stop / learned
- **The pixel suite masks `img`/`picture`** - SVG exhibit changes are invisible to the screenshot tests (qtest "nothing to run", no baseline moves). Verification for SVG waves is rendered-review + source-consistency, NOT the pixel suite. Same class as the W1.6 hero-fold gap: know what the visual suite does NOT cover.
- **O8 (worktree node_modules) hit every W3 worker** - each ran bun install / symlinked to get hugo-build green. Confirmed the strongest 20%-slot candidate; implement at the W4 boundary.

### Action into W4
1. [ ] Implement O8 (worktree node_modules auto-setup) as the W4 20%-slot - removes the friction every fan-out hits.
2. [ ] W4 is post-Aug-14 (campaign freeze) - groom-and-park until the window opens; it carries the W2 responsive-table CSS fix + the item-16 landing full-migration, both under the new design-call method (panel + competitor grounding, decided autonomously).
3. [ ] M2-M5 SVG→O2 rollout is a distinct later wave (ADR-gated to follow the M1 pilot) - do not fold into W4.

---

## M2-M5 O2 rollout retrospective (2026-08-02, brief)

Scope: PR #434, 17 SVGs in 3 sub-waves (Opus). Extends W3's M1 O2 pilot.

- **Continue**: spec+template-first (from W3) made 3 sub-wave agents produce
  consistent output with zero style drift; per-SW structural sweep by main
  (viewBox/floor/no-Caveat) caught nothing because the shared template held.
  The redraws also DELETED 2 fabricated stats - conversion work doubles as a
  voice-cleanup pass on legacy exhibits.
- **Stop/learned**: dtest flaked EXIT:1 with zero diff artifacts (aborted
  before compare, transient env/OOM) - do NOT read a bare non-zero dtest exit
  as a real regression; re-run clean and read the assertion counts (it was
  34/34). Same transient class as the W1.6 exit-137. Capture full dtest logs,
  not `tail -3` (lost the verdict twice this session).
- **Root-cause win**: the defect (74/80 SVGs under-floor) existed because the
  only visual gate masks `<img>`. O7b (`bin/check-svg-floor`) closes that -
  legibility is now measurable/enforceable, and it produced the exact 46-SVG
  burn-down for item 18. Fixing the GATE, not just the instances.
- **Action**: item 18 (46 deferred SVGs) is queued; flip check-svg-floor to
  blocking once it hits zero. Next executable wave = W5 (completion mechanics,
  content-safe; tracker piece Clarity-gated) - or interleave item 18.
