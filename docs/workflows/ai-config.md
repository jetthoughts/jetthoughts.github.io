# AI Configuration Flow

Use this flow for agents, skills, and CLI configuration.

1. Read `@docs/workflows/BASE_HANDBOOK.md`
2. Prefer skills over agents; keep agents thin
3. Sync skills with `bin/skillshare-sync` when changing skills or targets
4. Keep instruction files compact and refer to workflows via `@` pointers
